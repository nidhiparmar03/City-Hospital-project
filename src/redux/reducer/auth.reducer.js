const initVal = {
    isLoading: false,
    user: null,
    error: null,
}


export const authReducer = (state = initVal, action) => {
    switch (action.type) {
        default :
        return  state;
    }
};